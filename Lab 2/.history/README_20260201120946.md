# GIẢI THÍCH CODE TCP/UDP

## 📋 MỤC LỤC
- [Bài 1: Giải phương trình bậc 2](#bài-1-giải-phương-trình-bậc-2)
- [Bài 2: Nhân hai ma trận](#bài-2-nhân-hai-ma-trận)
- [Bài 3: Thay thế chuỗi trong file](#bài-3-thay-thế-chuỗi-trong-file)
- [Bài 4: Xử lý chuỗi](#bài-4-xử-lý-chuỗi)
- [Bài 5: Chat room](#bài-5-chat-room)

---

## Bài 1: Giải phương trình bậc 2

### 📁 File: `1_tcp_server.js`

```javascript
// Dòng 1: Import module 'net' để làm việc với TCP socket
const net = require('net');

// Dòng 3-14: Hàm giải phương trình ax² + bx + c = 0
function solve(a, b, c) {
    // Dòng 4: Tính delta theo công thức Δ = b² - 4ac
    const delta = b * b - 4 * a * c;
    
    // Dòng 5: Nếu delta < 0 → phương trình vô nghiệm (không có nghiệm thực)
    if (delta < 0) return 'Vô nghiệm';
    
    // Dòng 6-9: Nếu delta = 0 → nghiệm kép
    if (delta === 0) {
        // Dòng 7: Tính nghiệm kép x = -b / (2a)
        const x = -b / (2 * a);
        // Dòng 8: Trả về kết quả, toFixed(2) làm tròn 2 chữ số thập phân
        return `Nghiệm kép: x = ${x.toFixed(2)}`;
    }
    
    // Dòng 10-11: Nếu delta > 0 → 2 nghiệm phân biệt
    // Math.sqrt(delta) tính căn bậc 2 của delta
    const x1 = (-b + Math.sqrt(delta)) / (2 * a);  // Nghiệm 1
    const x2 = (-b - Math.sqrt(delta)) / (2 * a);  // Nghiệm 2
    // Dòng 12: Trả về cả 2 nghiệm
    return `x1 = ${x1.toFixed(2)}, x2 = ${x2.toFixed(2)}`;
}

// Dòng 16: Tạo TCP server
// net.createServer() nhận callback được gọi mỗi khi có client kết nối
// Tham số 'socket' là kết nối với client đó
const server = net.createServer((socket) => {
    // Dòng 17: In thông báo khi có client kết nối
    console.log('Client kết nối!');
    
    // Dòng 18: Lắng nghe sự kiện 'data' - khi nhận dữ liệu từ client
    socket.on('data', (data) => {
        // Dòng 19: Chuyển Buffer thành String và xóa khoảng trắng đầu/cuối
        const input = data.toString().trim();
        
        // Dòng 20: Tách chuỗi theo dấu cách, chuyển mỗi phần tử thành số
        // Ví dụ: "1 -5 6" → ["1", "-5", "6"] → [1, -5, 6]
        const nums = input.split(' ').map(Number);
        
        // Dòng 21: Destructuring - lấy 3 phần tử đầu làm a, b, c
        const [a, b, c] = nums;
        
        // Dòng 23: In ra phương trình nhận được
        console.log(`Nhận: ${a}x² + ${b}x + ${c} = 0`);
        
        // Dòng 24: Gọi hàm solve để giải phương trình
        const result = solve(a, b, c);
        
        // Dòng 25: Gửi kết quả về client qua socket
        socket.write(result);
        
        // Dòng 26: In kết quả đã gửi
        console.log(`Gửi: ${result}\n`);
    });
});

// Dòng 30: Cho server lắng nghe trên cổng 5000
// Callback được gọi khi server sẵn sàng
server.listen(5000, () => {
    console.log('Server chạy tại cổng 5000\n');
});
```

### 📁 File: `1_tcp_client.js`

```javascript
// Dòng 1-2: Import các module cần thiết
const net = require('net');              // Module TCP socket
const readline = require('readline');    // Module đọc input từ console

// Dòng 3: Tạo một socket client để kết nối đến server
const client = new net.Socket();

// Dòng 5-8: Tạo interface để đọc input từ bàn phím
const rl = readline.createInterface({
    input: process.stdin,    // Đọc từ bàn phím
    output: process.stdout   // Xuất ra console
});

// Dòng 10: Kết nối đến server
// Tham số: (port, host, callback khi kết nối thành công)
client.connect(5000, '127.0.0.1', () => {
    // Dòng 11-12: In thông báo khi kết nối thành công
    console.log('Kết nối thành công!');
    console.log('Nhập 3 số a b c (cách nhau bởi dấu cách)\n');
    
    // Dòng 13: Gọi hàm để bắt đầu nhập dữ liệu
    askInput();
});

// Dòng 16-20: Hàm hỏi người dùng nhập 3 số
function askInput() {
    // Dòng 17: Hiển thị prompt và đợi người dùng nhập
    rl.question('Nhập a b c: ', (input) => {
        // Dòng 18: Khi người dùng nhấn Enter, gửi dữ liệu đến server
        client.write(input);
    });
}

// Dòng 22-25: Lắng nghe dữ liệu từ server
client.on('data', (data) => {
    // Dòng 23: In kết quả nhận được từ server
    console.log(`→ ${data}\n`);
    
    // Dòng 24: Hỏi tiếp để nhập bộ số mới
    askInput();
});

// Dòng 27-30: Xử lý khi kết nối bị đóng
client.on('close', () => {
    // Dòng 28: In thông báo
    console.log('Ngắt kết nối');
    // Dòng 29: Đóng readline interface
    rl.close();
});
```

---

## Bài 2: Nhân hai ma trận

### 📁 File: `2_tcp_server.js`

```javascript
// Dòng 1: Import module net
const net = require('net')

// Dòng 3-13: Hàm nhân 2 ma trận
// Tham số: a = ma trận A[m×n], b = ma trận B[n×p]
// Kết quả: ma trận C[m×p]
function solve(a, b){
    // Dòng 4: Khởi tạo mảng kết quả rỗng
    const res = [];
    
    // Dòng 5: Vòng lặp qua từng hàng của ma trận A
    for(let i=0; i<a.length; ++i){
        // Dòng 6: Tạo hàng thứ i của kết quả
        res[i] = [];
        
        // Dòng 7: Vòng lặp qua từng cột của ma trận B
        for(let j=0; j<b[0].length; ++j){
            // Dòng 8: Khởi tạo phần tử res[i][j] = 0
            res[i][j] = 0;
            
            // Dòng 9-10: Tính tổng tích
            // res[i][j] = a[i][0]*b[0][j] + a[i][1]*b[1][j] + ... + a[i][k]*b[k][j]
            for(let k=0; k<b.length; ++k)
                res[i][j] += a[i][k]*b[k][j];  // Nhân và cộng dồn
        }
    }
    // Dòng 12: Trả về ma trận kết quả
    return res; 
}

// Dòng 15: Tạo TCP server
const server = net.createServer((socket) => {
    // Dòng 16: In thông báo khi client kết nối
    console.log('Client connect');
    
    // Dòng 17: Lắng nghe dữ liệu từ client
    socket.on('data', (data) => {
        // Dòng 18: Parse JSON thành object
        // Client gửi dạng: {"m1": [[1,2],[3,4]], "m2": [[5,6],[7,8]]}
        const {m1, m2} = JSON.parse(data);
        
        // Dòng 19: Gọi hàm nhân ma trận
        const ans = solve(m1, m2);
        
        // Dòng 20: In kết quả ra console
        console.log('Ket qua', ans, '\n');
        
        // Dòng 21: Chuyển kết quả thành JSON string và gửi về client
        socket.write(JSON.stringify(ans));
        
        // Dòng 22: In thông báo đã gửi
        console.log('Gui');
    });
});

// Dòng 26: Lắng nghe trên cổng 5000
server.listen(5000, () => {
    console.log('Serer chay ...')
});
```

### 📁 File: `2_tcp_client.js`

```javascript
// Dòng 1-2: Import module
const net = require('net');
const readline = require('readline');

// Dòng 4: Tạo socket client
const client = new net.Socket();

// Dòng 5: Tạo readline interface
const rl = readline.createInterface({ input: process.stdin, output: process.stdout });

// Dòng 7-16: Hàm hiển thị ma trận đẹp
function show(m, name) {
    // Dòng 8: In tên ma trận
    console.log(`${name}:`);
    
    // Dòng 9: Vòng lặp qua từng hàng
    for(let i=0; i<m.length; i++){
        // Dòng 10: Tạo chuỗi cho 1 hàng, bắt đầu bằng 2 dấu cách
        let row = '  ';
        
        // Dòng 11: Vòng lặp qua từng cột
        for(let j=0; j<m[i].length; j++){
            // Dòng 12: Thêm giá trị vào chuỗi, cách nhau 2 khoảng trắng
            row += m[i][j] + '  ';
        }
        // Dòng 14: In chuỗi hàng ra console
        console.log(row);
    }
}

// Dòng 17-31: Hàm nhập ma trận từ người dùng
// Tham số: name = tên ma trận, cb = callback function
function input(name, cb){
    // Dòng 18: Hỏi kích thước ma trận (số hàng r, số cột c)
    rl.question(`${name} (Kích thước n, m): `, (size) => {
        // Dòng 19: Parse thành 2 số
        const [r, c] = size.split(' ').map(Number);
        
        // Dòng 20: Hỏi nhập tất cả các số (r*c số)
        rl.question(`Nhập ${r*c} số: `, (line) => {
            // Dòng 21: Tách chuỗi thành mảng số
            const n = line.split(' ').map(Number);
            
            // Dòng 22: Khởi tạo ma trận rỗng
            const m = [];
            
            // Dòng 23: Vòng lặp tạo từng hàng
            for(let i=0; i<r; i++){
                // Dòng 24: Tạo hàng thứ i
                m[i] = [];
                
                // Dòng 25: Vòng lặp tạo từng cột
                for(let j=0; j<c; j++){
                    // Dòng 26: Lấy số từ mảng n theo công thức: vị trí = i*c + j
                    // Ví dụ r=2, c=2: [1,2,3,4] → [[1,2],[3,4]]
                    m[i][j] = n[i*c + j];
                }
            }
            // Dòng 29: Gọi callback với ma trận đã tạo
            cb(m);
        });
    });
}

// Dòng 33-36: Kết nối đến server
client.connect(5000, '127.0.0.1', () => {
    // Dòng 34: In thông báo kết nối OK
    console.log('OK\n');
    // Dòng 35: Bắt đầu nhập liệu
    ask();
});

// Dòng 38-44: Hàm điều khiển luồng nhập liệu
function ask(){
    // Dòng 39: Nhập ma trận A
    input('Ma trận A : ', (m1) => {
        // Dòng 40: Sau khi nhập xong A, nhập ma trận B
        input('Ma trận B', (m2) => {
            // Dòng 41: In thông báo
            console.log('\nGửi lên server ...');
            
            // Dòng 42: Gửi 2 ma trận dạng JSON đến server
            client.write(JSON.stringify({ m1, m2 }));
        });
    });
}

// Dòng 46-51: Nhận kết quả từ server
client.on('data', (data) => {
    // Dòng 47: Parse JSON thành ma trận và hiển thị
    show(JSON.parse(data), 'Kết quả ma trận A×B');
    
    // Dòng 48: Hỏi có muốn tiếp tục không
    rl.question('\nTiếp? (y/n): ', (a) => {
        // Dòng 49: Nếu nhập 'y', tiếp tục
        if (a === 'y') ask();
        // Dòng 50: Nếu không, đóng kết nối
        else { client.destroy(); rl.close(); }
    });
});
```

---

## Bài 3: Thay thế chuỗi trong file

### 📁 File: `3_tcp_server.js`

```javascript
// Dòng 1: Import module net
const net = require('net');

// Dòng 3-5: Hàm thay thế tất cả s1 thành s2 trong content
function replaceAll(content, s1, s2) {
    // Dòng 4: Split chuỗi theo s1, rồi join lại bằng s2
    // Ví dụ: "Hello world Hello" với s1="Hello", s2="Hi"
    // → ["", " world ", ""] → "Hi world Hi"
    return content.split(s1).join(s2);
}

// Dòng 7: Tạo TCP server
const server = net.createServer((socket) => {
    // Dòng 8: In thông báo
    console.log('Client kết nối!\n');
    
    // Dòng 10: Lắng nghe dữ liệu từ client
    socket.on('data', (data) => {
        // Dòng 11-12: Parse JSON để lấy file content và 2 chuỗi
        const request = JSON.parse(data.toString());
        const { fileContent, s1, s2 } = request;
        
        // Dòng 14-15: In thông tin nhận được
        console.log(`Nhận file (${fileContent.length} ký tự)`);
        console.log(`Thay thế: "${s1}" → "${s2}"`);
        
        // Dòng 16: Gọi hàm thay thế
        const result = replaceAll(fileContent, s1, s2);
        
        // Dòng 17: Đếm số lần xuất hiện của s1
        // Cách đếm: split theo s1 sẽ tạo ra (n+1) phần, với n là số lần xuất hiện
        const count = (fileContent.split(s1).length - 1);
        
        // Dòng 19: In số lần thay thế
        console.log(`Đã thay thế ${count} lần\n`);
        
        // Dòng 20: Gửi kết quả về client dạng JSON
        socket.write(JSON.stringify({ result, count }));
    });
});

// Dòng 24: Lắng nghe trên cổng 5002
server.listen(5002, () => {
    console.log('Server chạy tại cổng 5002\n');
});
```

---

## Bài 4: Xử lý chuỗi

### 📁 File: `4_tcp_server.js`

```javascript
// Dòng 1: Import module net
const net = require('net');

// Dòng 3-23: Hàm xử lý các lệnh khác nhau
// Tham số: cmd = tên lệnh, text = chuỗi cần xử lý, ...params = các tham số thêm
function processCommand(cmd, text, ...params) {
    // Dòng 4: Switch-case kiểm tra lệnh
    switch(cmd) {
        // Dòng 5-6: Lệnh UPPER - chuyển thành chữ HOA
        case 'UPPER':
            return text.toUpperCase();
        
        // Dòng 7-8: Lệnh LOWER - chuyển thành chữ thường
        case 'LOWER':
            return text.toLowerCase();
        
        // Dòng 9-12: Lệnh DELETE - xóa 1 đoạn chuỗi
        case 'DELETE':
            // Dòng 11: Lấy vị trí bắt đầu và kết thúc từ params
            const [start, end] = params.map(Number);
            // Dòng 12: Cắt 2 đoạn và nối lại
            // Ví dụ: "hello world" với start=0, end=5 → "" + " world" = " world"
            return text.slice(0, start) + text.slice(end);
        
        // Dòng 13-17: Lệnh INSERT - chèn chuỗi con
        case 'INSERT':
            // Dòng 15-16: Lấy vị trí và chuỗi con cần chèn
            const pos = Number(params[0]);
            const substr = params[1];
            // Dòng 17: Cắt làm 2 đoạn, chèn substr vào giữa
            // Ví dụ: "hello" với pos=5, substr=" world" → "hello" + " world" + "" = "hello world"
            return text.slice(0, pos) + substr + text.slice(pos);
        
        // Dòng 18-19: Lệnh không hợp lệ
        default:
            return 'Lệnh không hợp lệ';
    }
}

// Dòng 22: Tạo TCP server
const server = net.createServer((socket) => {
    // Dòng 23: In thông báo
    console.log('Client kết nối!\n');
    
    // Dòng 25: Lắng nghe dữ liệu từ client
    socket.on('data', (data) => {
        // Dòng 26-27: Parse dữ liệu
        // Format: "UPPER|hello world" → ["UPPER", "hello world"]
        const parts = data.toString().split('|');
        // Destructuring: phần tử đầu là cmd, thứ 2 là text, còn lại là params
        const [cmd, text, ...params] = parts;
        
        // Dòng 29-31: In thông tin nhận được
        console.log(`Lệnh: ${cmd}`);
        console.log(`Text: ${text}`);
        console.log(`Params: ${params.join(', ')}`);
        
        // Dòng 33: Gọi hàm xử lý lệnh
        const result = processCommand(cmd, text, ...params);
        // Dòng 34: In kết quả
        console.log(`→ ${result}\n`);
        
        // Dòng 36: Gửi kết quả về client
        socket.write(result);
    });
});

// Dòng 40-42: Lắng nghe trên cổng 5003
server.listen(5003, () => {
    console.log('Server chạy cổng 5003');
    console.log('Lệnh: UPPER, LOWER, DELETE, INSERT\n');
});
```

---

## Bài 5: Chat room

### 📁 File: `5_tcp_server.js`

```javascript
// Dòng 1-2: Import module
const net = require('net');
const readline = require('readline');

// Dòng 4: Mảng lưu tất cả client đang kết nối
const clients = [];

// Dòng 5: Tạo TCP server
const server = net.createServer((socket) => {
    
    // Dòng 7: Gửi yêu cầu nhập tên cho client mới
    socket.write('Nhập tên của bạn: ');
    
    // Dòng 8-9: Biến lưu trạng thái
    let username = '';     // Tên người dùng
    let isNamed = false;   // Đã nhập tên chưa
    
    // Dòng 11: Lắng nghe dữ liệu từ client
    socket.on('data', (data) => {
        // Dòng 12: Lấy message và xóa khoảng trắng
        const msg = data.toString().trim();
        
        // Dòng 14-23: Nếu chưa nhập tên
        if (!isNamed) {
            // Dòng 15: Lưu tên
            username = msg;
            
            // Dòng 16: Thêm client vào danh sách
            clients.push({ socket, username });
            
            // Dòng 17: In thông báo server
            console.log(`[+] ${username} tham gia (${clients.length} online)\n`);
            
            // Dòng 19: Thông báo cho tất cả client khác
            broadcast(`[SERVER] ${username} đã tham gia chat!`, socket);
            
            // Dòng 20: Gửi lời chào cho client vừa vào
            socket.write(`Chào ${username}! Gõ tin nhắn để chat.\n\n`);
            
            // Dòng 21-22: Đánh dấu đã nhập tên, thoát khỏi hàm
            isNamed = true;
            return;
        }
        
        // Dòng 25-26: Nếu đã nhập tên → xử lý tin nhắn chat
        // Format tin nhắn: "[username]: message"
        const message = `[${username}]: ${msg}`;
        
        // Dòng 27: In tin nhắn ra server console
        console.log(message);
        
        // Dòng 28: Gửi tin nhắn cho tất cả client (trừ người gửi)
        broadcast(message, socket);
        
        // Dòng 30-31: Xác nhận đã gửi cho người gửi
        socket.write('✓ Đã gửi\n');
    });
    
    // Dòng 34-41: Xử lý khi client ngắt kết nối
    socket.on('close', () => {
        // Dòng 35-36: Tìm vị trí của client trong mảng
        const index = clients.findIndex(c => c.socket === socket);
        
        // Dòng 37: Nếu tìm thấy
        if (index !== -1) {
            // Dòng 38: Xóa khỏi mảng (1 phần tử tại vị trí index)
            clients.splice(index, 1);
            
            // Dòng 39: In thông báo server
            console.log(`[-] ${username} rời khỏi (${clients.length} online)\n`);
            
            // Dòng 40: Thông báo cho tất cả client còn lại
            broadcast(`[SERVER] ${username} đã rời khỏi!`);
        }
    });
});

// Dòng 44-50: Hàm broadcast - gửi tin nhắn cho tất cả client
// Tham số: message = nội dung, exclude = socket cần loại trừ (không gửi)
function broadcast(message, exclude = null) {
    // Dòng 45: Duyệt qua tất cả client
    clients.forEach(client => {
        // Dòng 46-48: Nếu không phải client cần loại trừ
        if (client.socket !== exclude) {
            // Gửi tin nhắn
            client.socket.write(message + '\n');
        }
    });
}

// Dòng 52-57: Lắng nghe trên cổng 5004
server.listen(5004, () => {
    console.log('='.repeat(40));
    console.log('  CHAT ROOM SERVER - TCP');
    console.log('  Port: 5004');
    console.log('='.repeat(40) + '\n');
});
```

---

## 🔑 Các khái niệm quan trọng

### 1. **Callback Function**
```javascript
// Hàm được truyền như tham số, sẽ được gọi sau
function input(name, cb) {
    rl.question('Nhập: ', (answer) => {
        cb(answer);  // Gọi callback với kết quả
    });
}
```

### 2. **Arrow Function**
```javascript
// Cú pháp ngắn gọn
(param) => { return value }

// Tương đương với:
function(param) { return value }
```

### 3. **Destructuring**
```javascript
// Lấy giá trị từ object
const { m1, m2 } = { m1: [[1,2]], m2: [[3,4]] };
// → m1 = [[1,2]], m2 = [[3,4]]

// Lấy giá trị từ array
const [a, b, c] = [1, 2, 3];
// → a = 1, b = 2, c = 3
```

### 4. **Spread Operator (...)**
```javascript
function test(cmd, text, ...params) {
    // params là mảng chứa tất cả tham số còn lại
}
test('UPPER', 'hello', 1, 2, 3);  // params = [1, 2, 3]
```

### 5. **JSON**
```javascript
// Object → String
JSON.stringify({ a: 1, b: 2 });  // '{"a":1,"b":2}'

// String → Object
JSON.parse('{"a":1,"b":2}');  // { a: 1, b: 2 }
```

### 6. **Buffer**
```javascript
// Socket nhận/gửi dữ liệu dạng Buffer
data.toString()          // Chuyển Buffer thành String
Buffer.from('hello')     // Chuyển String thành Buffer
```

---

## 📝 Cách chạy các file

```bash
# TCP
node 1_tcp_server.js  # Terminal 1
node 1_tcp_client.js  # Terminal 2

# UDP (tương tự)
node 1_udp_server.js
node 1_udp_client.js
```

**Lưu ý:** Luôn chạy server trước, rồi mới chạy client!

---

## Kiến thức nền tảng

### 🌐 Mô hình Client-Server

```
┌──────────┐                      ┌──────────┐
│  Client  │  ──── Request ────>  │  Server  │
│          │                      │          │
│          │  <─── Response ────  │          │
└──────────┘                      └──────────┘
```

**Nguyên lý hoạt động:**
1. **Server**: Lắng nghe (listen) trên một port
2. **Client**: Kết nối (connect) đến server
3. **Communication**: Trao đổi dữ liệu 2 chiều
4. **Close**: Đóng kết nối khi hoàn thành

### 📡 TCP (Transmission Control Protocol)

**Đặc điểm:**
- **Connection-oriented**: Cần thiết lập kết nối trước (3-way handshake)
- **Reliable**: Đảm bảo dữ liệu đến đích đầy đủ, đúng thứ tự
- **Flow control**: Kiểm soát tốc độ gửi/nhận
- **Error checking**: Kiểm tra lỗi và gửi lại nếu mất gói

**Cơ chế 3-way handshake:**
```
Client                          Server
  │                               │
  │─────── SYN ──────────────────>│
  │                               │
  │<────── SYN-ACK ───────────────│
  │                               │
  │─────── ACK ──────────────────>│
  │                               │
  │      Connection Established   │
```

**Khi nào dùng TCP:**
- ✅ Truyền file (FTP, HTTP)
- ✅ Email (SMTP, POP3)
- ✅ Chat, messaging
- ✅ Remote access (SSH, Telnet)

### 📡 UDP (User Datagram Protocol)

**Đặc điểm:**
- **Connectionless**: Không cần thiết lập kết nối
- **Unreliable**: Không đảm bảo dữ liệu đến đích
- **Fast**: Nhanh hơn TCP (ít overhead)
- **No ordering**: Gói tin có thể đến không đúng thứ tự

**Cơ chế gửi nhận:**
```
Client                          Server
  │                               │
  │─────── Data ─────────────────>│  (có thể mất)
  │                               │
  │─────── Data ─────────────────>│  (có thể đến)
  │                               │
  │       No acknowledgment       │
```

**Khi nào dùng UDP:**
- ✅ Video streaming (YouTube, Netflix)
- ✅ Game online (FPS, MOBA)
- ✅ VoIP (Skype, Zoom)
- ✅ DNS queries

---

## Bài 1: Giải phương trình bậc 2

### 📝 Mô tả
Client gửi 3 số `a, b, c` đến server. Server giải phương trình `ax² + bx + c = 0` và gửi kết quả về client.

### 🚀 Cách chạy

**TCP:**
```bash
# Terminal 1
node 1_tcp_server.js

# Terminal 2
node 1_tcp_client.js
```

**UDP:**
```bash
# Terminal 1
node 1_udp_server.js

# Terminal 2
node 1_udp_client.js
```

### 💡 Ví dụ
```
Nhập a b c: 1 -5 6
→ x1 = 3.00, x2 = 2.00

Nhập a b c: 1 -4 4
→ Nghiệm kép: x = 2.00

Nhập a b c: 1 2 5
→ Vô nghiệm
```

### 🔍 Giải thích chi tiết code

#### **Server TCP - Phân tích từng dòng:**

```javascript
// 1. Import module net để làm việc với TCP
const net = require('net');

// 2. Hàm giải phương trình bậc 2: ax² + bx + c = 0
function solve(a, b, c) {
    // Công thức delta: Δ = b² - 4ac
    const delta = b * b - 4 * a * c;
    
    // Trường hợp 1: delta < 0 → Vô nghiệm (không có nghiệm thực)
    if (delta < 0) return 'Vô nghiệm';
    
    // Trường hợp 2: delta = 0 → Nghiệm kép
    // Công thức: x = -b / 2a
    if (delta === 0) {
        const x = -b / (2 * a);
        return `Nghiệm kép: x = ${x.toFixed(2)}`;
    }
    
    // Trường hợp 3: delta > 0 → 2 nghiệm phân biệt
    // Công thức: x = (-b ± √Δ) / 2a
    const x1 = (-b + Math.sqrt(delta)) / (2 * a);
    const x2 = (-b - Math.sqrt(delta)) / (2 * a);
    return `x1 = ${x1.toFixed(2)}, x2 = ${x2.toFixed(2)}`;
}

// 3. Tạo TCP server với callback được gọi khi có client kết nối
const server = net.createServer((socket) => {
    console.log('Client đã kết nối');
    
    // 4. Lắng nghe sự kiện 'data' - khi nhận dữ liệu từ client
    socket.on('data', (data) => {
        // 5. Chuyển Buffer thành String
        const input = data.toString();
        console.log(`Nhận: ${input}`);
        
        // 6. Parse chuỗi thành 3 số
        // Ví dụ: "1 -5 6" → [1, -5, 6]
        const [a, b, c] = input.split(' ').map(Number);
        
        // 7. Giải phương trình
        const result = solve(a, b, c);
        console.log(`Gửi: ${result}`);
        
        // 8. Gửi kết quả về client
        socket.write(result);
    });
    
    // 9. Xử lý khi client ngắt kết nối
    socket.on('close', () => {
        console.log('Client đã ngắt kết nối\n');
    });
    
    // 10. Xử lý lỗi
    socket.on('error', (err) => {
        console.error('Lỗi:', err.message);
    });
});

// 11. Lắng nghe trên port 5000, IP 127.0.0.1 (localhost)
server.listen(5000, '127.0.0.1', () => {
    console.log('Server đang chạy trên 127.0.0.1:5000');
});
```

#### **Client TCP - Phân tích từng dòng:**

```javascript
const net = require('net');
const readline = require('readline');

// 1. Tạo socket client
const client = new net.Socket();

// 2. Tạo interface để đọc input từ console
const rl = readline.createInterface({
    input: process.stdin,   // Đọc từ bàn phím
    output: process.stdout  // Xuất ra console
});

// 3. Kết nối đến server
// Tham số: (port, host, callback)
client.connect(5000, '127.0.0.1', () => {
    console.log('✓ Đã kết nối đến server\n');
    askInput();  // Bắt đầu nhập liệu
});

// 4. Hàm hỏi người dùng nhập dữ liệu
function askInput() {
    rl.question('Nhập a b c: ', (input) => {
        // Gửi dữ liệu đến server
        client.write(input);
    });
}

#### **Lý thuyết toán học:**

**Điều kiện nhân ma trận:**
- Ma trận `A[m×n]` × Ma trận `B[n×p]` = Ma trận `C[m×p]`
- **Quan trọng:** Số cột của A phải bằng số hàng của B

**Công thức:** 
```
C[i][j] = Σ(A[i][k] × B[k][j])  với k = 0 → n-1
```

**Minh họa trực quan:**
```
    A (2×3)          B (3×2)       C (2×2)
┌─────────────┐  ┌─────────┐   ┌─────────┐
│  1   2   3  │  │  7   8  │   │ 58  64  │
│  4   5   6  │  │  9  10  │   │139 154  │
└─────────────┘  │ 11  12  │   └─────────┘
                 └─────────┘

C[0][0] = 1×7 + 2×9 + 3×11 = 7 + 18 + 33 = 58
C[0][1] = 1×8 + 2×10 + 3×12 = 8 + 20 + 36 = 64
C[1][0] = 4×7 + 5×9 + 6×11 = 28 + 45 + 66 = 139
C[1][1] = 4×8 + 5×10 + 6×12 = 32 + 50 + 72 = 154
```

#### **Code chi tiết với comments:**

```javascript
/**
 * Nhân hai ma trận
 * @param {number[][]} A - Ma trận A có kích thước m×n
 * @param {number[][]} B - Ma trận B có kích thước n×p
 * @returns {number[][]} - Ma trận C có kích thước m×p
 */
function multiply(A, B) {
    // 1. Lấy kích thước các ma trận
    const m = A.length;           // Số hàng của A
    const n = A[0].length;        // Số cột của A
    const p = B[0].length;        // Số cột của B
    
    // 2. Kiểm tra điều kiện nhân được
    if (n !== B.length) {
        throw new Error('Không thể nhân: số cột A ≠ số hàng B');
    }
    
    // 3. Khởi tạo ma trận kết quả C[m×p]
    const C = [];
    
    // 4. Vòng lặp qua từng hàng của C
    for (let i = 0; i < m; i++) {
        C[i] = [];  // Khởi tạo hàng i
        
        // 5. Vòng lặp qua từng cột của C
        for (let j = 0; j < p; j++) {
            C[i][j] = 0;  // Khởi tạo phần tử = 0
            
            // 6. Tính tổng tích: C[i][j] = Σ(A[i][k] × B[k][j])
            for (let k = 0; k < n; k++) {
                // Lấy phần tử hàng i của A
                // Lấy phần tử cột j của B
                // Nhân và cộng dồn
                C[i][j] += A[i][k] * B[k][j];
                
                // Debug: in từng bước tính
                // console.log(`C[${i}][${j}] += A[${i}][${k}] × B[${k}][${j}]`);
                // console.log(`C[${i}][${j}] += ${A[i][k]} × ${B[k][j]} = ${A[i][k] * B[k][j]}`);
            }
        }
    }
    
    return C;
}
```

#### **Ví dụ chi tiết từng bước:**

```javascript
// Ma trận A (2×2)
const A = [
    [1, 2],
    [3, 4]
];

// Ma trận B (2×2)
const B = [
    [5, 6],
    [7, 8]
];

// Tính C[0][0]:
// C[0][0] = A[0][0] × B[0][0] + A[0][1] × B[1][0]
//         = 1 × 5 + 2 × 7
//         = 5 + 14
//         = 19

// Tính C[0][1]:
// C[0][1] = A[0][0] × B[0][1] + A[0][1] × B[1][1]
//         = 1 × 6 + 2 × 8
//         = 6 + 16
//         = 22

// Tính C[1][0]:
// C[1][0] = A[1][0] × B[0][0] + A[1][1] × B[1][0]
//         = 3 × 5 + 4 × 7
//         = 15 + 28
//         = 43

// Tính C[1][1]:
// C[1][1] = A[1][0] × B[0][1] + A[1][1] × B[1][1]
//         = 3 × 6 + 4 × 8
//         = 18 + 32
//         = 50

// Kết quả:
const C = [
    [19, 22],
    [43, 50]
];
```

### 🔍 Cách gửi ma trận qua mạng

#### **Phương pháp 1: JSON (Khuyến nghị)**

**Ưu điểm:**
- ✅ Dễ hiểu, dễ debug
- ✅ Hỗ trợ nhiều kiểu dữ liệu
- ✅ Human-readable

**Nhược điểm:**
- ❌ Tốn bandwidth (kích thước lớn)
- ❌ Chậm hơn binary

```javascript
// ========== CLIENT SIDE ==========
const m1 = [[1, 2], [3, 4]];
const m2 = [[5, 6], [7, 8]];

// 1. Serialize thành JSON string
const payload = JSON.stringify({
    m1: m1,
    m2: m2
});

// 2. Gửi qua socket
socket.write(payload);

// Kích thước: ~60 bytes
// {"m1":[[1,2],[3,4]],"m2":[[5,6],[7,8]]}


// ========== SERVER SIDE ==========
socket.on('data', (data) => {
    // 1. Parse JSON string thành object
    const request = JSON.parse(data.toString());
    
    // 2. Lấy ra 2 ma trận
    const { m1, m2 } = request;
    
    // 3. Xử lý
    const result = multiply(m1, m2);
    
    // 4. Gửi lại kết quả
    const response = JSON.stringify({
        result: result,
        status: 'success'
    });
    
    socket.write(response);
});

// CLIENT nhận kết quả
client.on('data', (data) => {
    const response = JSON.parse(data.toString());
    console.log(response.result);
    // [[19, 22], [43, 50]]
});
```

#### **Phương pháp 2: Binary (Nâng cao)**

**Ưu điểm:**
- ✅ Nhỏ gọn, tiết kiệm bandwidth
- ✅ Nhanh hơn

**Nhược điểm:**
- ❌ Khó debug
- ❌ Phải biết chính xác format

```javascript
// ========== CLIENT SIDE ==========
function serializeMatrix(m1, m2) {
    // Format: [r1, c1, ...data1, r2, c2, ...data2]
    const r1 = m1.length;
    const c1 = m1[0].length;
    const r2 = m2.length;
    const c2 = m2[0].length;
    
    // Tính tổng số phần tử
    const totalSize = 4 + (r1 * c1) + (r2 * c2);
    
    // Tạo Buffer (mỗi số = 4 bytes = Int32)
    const buffer = Buffer.alloc(totalSize * 4);
    
    let offset = 0;
    
    // Ghi kích thước ma trận 1
    buffer.writeInt32LE(r1, offset); offset += 4;
    buffer.writeInt32LE(c1, offset); offset += 4;
    
    // Ghi dữ liệu ma trận 1
    for (let i = 0; i < r1; i++) {
        for (let j = 0; j < c1; j++) {
            buffer.writeInt32LE(m1[i][j], offset);
            offset += 4;
        }
    }
    
    // Ghi kích thước ma trận 2
    buffer.writeInt32LE(r2, offset); offset += 4;
    buffer.writeInt32LE(c2, offset); offset += 4;
    
    // Ghi dữ liệu ma trận 2
    for (let i = 0; i < r2; i++) {
        for (let j = 0; j < c2; j++) {
            buffer.writeInt32LE(m2[i][j], offset);
            offset += 4;
        }
    }
    
    return buffer;
}

// Gửi
const buffer = serializeMatrix(m1, m2);
socket.write(buffer);
// Kích thước: 72 bytes (18 số × 4 bytes)


// ========== SERVER SIDE ==========
socket.on('data', (buffer) => {
    let offset = 0;
    
    // Đọc kích thước ma trận 1
    const r1 = buffer.readInt32LE(offset); offset += 4;
    const c1 = buffer.readInt32LE(offset); offset += 4;
    
    // Đọc dữ liệu ma trận 1
    const m1 = [];
    for (let i = 0; i < r1; i++) {
        m1[i] = [];
        for (let j = 0; j < c1; j++) {
            m1[i][j] = buffer.readInt32LE(offset);
            offset += 4;
        }
    }
    
    // Tương tự cho ma trận 2...
});
```

#### **Phương pháp 3: CSV/Text (Đơn giản)**

```javascript
// CLIENT: Serialize thành text
const text = `${m1.length},${m1[0].length}\n` +
             m1.map(row => row.join(',')).join('\n') + '\n' +
             `${m2.length},${m2[0].length}\n` +
             m2.map(row => row.join(',')).join('\n');

socket.write(text);

// Format:
// 2,2
// 1,2
// 3,4
// 2,2
// 5,6
// 7,8

// SERVER: Parse text
const lines = data.toString().split('\n');
// ... parse lines ...
```

#### **So sánh các phương pháp:**

| Phương pháp | Kích thước | Tốc độ | Dễ dùng | Debug |
|-------------|-----------|--------|---------|-------|
| JSON | Lớn nhất | Chậm nhất | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ |
| Binary | Nhỏ nhất | Nhanh nhất | ⭐⭐ | ⭐ |
| CSV/Text | Trung bình | Trung bình | ⭐⭐⭐⭐ | ⭐⭐⭐⭐ |

**Khuyến nghị:** Dùng **JSON** cho học tập và dự án nhỏ server.send(result, rinfo.port, rinfo.address, (err) => {
        if (err) console.error('Lỗi gửi:', err);
    });
});

// 4. Bind (lắng nghe) trên port
server.bind(5000, () => {
    console.log('UDP Server đang chạy trên port 5000');
---

## API Reference

### 📚 TCP (module `net`)

#### **Server API:**

```javascript
const net = require('net');

// 1. Tạo server
const server = net.createServer([options], [connectionListener]);
```

**Options:**
- `allowHalfOpen` (boolean): Cho phép half-open TCP connections
- `pauseOnConnect` (boolean): Tạm dừng socket khi connect

**Events:**
- `connection`: Khi có client kết nối `(socket) => {}`
- `error`: Khi có lỗi `(err) => {}`
- `close`: Khi server đóng `() => {}`
- `listening`: Khi server bắt đầu lắng nghe `() => {}`

```javascript
// 2. Lắng nghe trên port
server.listen(port, [host], [backlog], [callback]);
```

**Parameters:**
- `port` (number): Port number (0-65535)
- `host` (string): IP address, mặc định '0.0.0.0' (tất cả interfaces)
- `backlog` (number): Max pending connections, mặc định 511
- `callback` (function): Gọi khi server sẵn sàng

```javascript
// 3. Đóng server
server.close([callback]);

// 4. Lấy thông tin server
server.address();  // { port: 5000, family: 'IPv4', address: '127.0.0.1' }

// 5. Số lượng kết nối hiện tại
server.getConnections((err, count) => {
    console.log(`${count} clients connected`);
});
```

#### **Socket API (server-side & client-side):**

```javascript
// 1. Gửi dữ liệu
socket.write(data, [encoding], [callback]);
// data: string hoặc Buffer
// encoding: 'utf8', 'ascii', 'base64', mặc định 'utf8'
// callback: gọi khi dữ liệu được gửi xong

// 2. Kết thúc gửi dữ liệu
socket.end([data], [encoding]);
// Gửi FIN packet, không thể gửi thêm

// 3. Đóng ngay lập tức
socket.destroy([error]);
// Đóng mà không đợi dữ liệu gửi xong

// 4. Tạm dừng/Tiếp tục nhận dữ liệu
socket.pause();  // Tạm dừng event 'data'
socket.resume(); // Tiếp tục nhận

// 5. Thiết lập timeout
socket.setTimeout(milliseconds, [callback]);
// Tự động đóng sau khoảng thời gian không hoạt động

// 6. Thiết lập keep-alive
socket.setKeepAlive([enable], [initialDelay]);

// 7. Vô hiệu hóa Nagle algorithm
socket.setNoDelay([noDelay]);
```

**Socket Properties:**
```javascript
socket.localAddress   // IP local
socket.localPort      // Port local
socket.remoteAddress  // IP remote
socket.remotePort     // Port remote
socket.bytesRead      // Số bytes đã đọc
socket.bytesWritten   // Số bytes đã ghi
socket.destroyed      // true nếu đã đóng
```

**Socket Events:**
```javascript
socket.on('data', (data) => {});     // Nhận dữ liệu
socket.on('close', (hadError) => {}); // Đóng kết nối
socket.on('error', (err) => {});      // Lỗi
socket.on('end', () => {});           // Phía bên kia đóng
socket.on('timeout', () => {});       // Timeout
socket.on('drain', () => {});         // Buffer trống, có thể ghi tiếp
```

#### **Client API:**

```javascript
// 1. Tạo socket
const client = new net.Socket();

// 2. Kết nối đến server
client.connect(port, [host], [connectListener]);
// hoặc
client.connect({ port, host }, [connectListener]);

// Ví dụ:
client.connect(5000, '127.0.0.1', () => {
    console.log('Connected!');
});
```

### 📚 UDP (module `dgram`)

#### **Socket API:**

```javascript
const dgram = require('dgram');

// 1. Tạo socket
const socket = dgram.createSocket(options, [callback]);
// hoặc
const socket = dgram.createSocket(type);

// type: 'udp4' (IPv4) hoặc 'udp6' (IPv6)
// options: { type, reuseAddr, ipv6Only, recvBufferSize, sendBufferSize }
```

**Methods:**
```javascript
// 2. Bind (lắng nghe) trên port
socket.bind([port], [address], [callback]);
// Mặc định: port = random, address = '0.0.0.0'

// 3. Gửi dữ liệu
socket.send(msg, [offset], [length], port, address, [callback]);
// msg: Buffer hoặc string
// offset, length: vị trí và độ dài trong buffer
// callback: (err) => {}

// Ví dụ:
const msg = Buffer.from('Hello');
socket.send(msg, 5000, '127.0.0.1', (err) => {
    if (err) console.error(err);
});

// 4. Đóng socket
socket.close([callback]);

// 5. Lấy thông tin
socket.address();  // { address, port, family }

// 6. Thiết lập broadcast
socket.setBroadcast(flag);  // true/false

// 7. Thiết lập multicast
socket.addMembership(multicastAddress, [multicastInterface]);
socket.dropMembership(multicastAddress, [multicastInterface]);
socket.setMulticastTTL(ttl);
socket.setMulticastLoopback(flag);
```

**Events:**
```javascript
socket.on('message', (msg, rinfo) => {});
// msg: Buffer
// rinfo: { address, port, family, size }

socket.on('error', (err) => {});
socket.on('close', () => {});
socket.on('listening', () => {});
```

---

## Best Practices

### ✅ Security Best Practices

#### **1. Validate Input**
```javascript
// ❌ BAD: Không validate
socket.on('data', (data) => {
    const nums = data.toString().split(' ').map(Number);
    const result = calculate(nums[0], nums[1]);  // Có thể undefined/NaN
});

// ✅ GOOD: Validate đầy đủ
socket.on('data', (data) => {
    try {
        const input = data.toString().trim();
        
        // Check length
        if (input.length > 1000) {
            throw new Error('Input quá dài');
        }
        
        // Parse và validate
        const nums = input.split(' ').map(Number);
        
        if (nums.length !== 2) {
            throw new Error('Phải nhập đúng 2 số');
        }
        
        if (nums.some(isNaN)) {
            throw new Error('Dữ liệu không hợp lệ');
        }
        
        const result = calculate(nums[0], nums[1]);
        socket.write(result.toString());
        
    } catch (err) {
        socket.write(`Lỗi: ${err.message}`);
    }
});
```

#### **2. Sanitize Data**
```javascript
// ❌ BAD: SQL Injection risk
const query = `SELECT * FROM users WHERE name = '${username}'`;

// ✅ GOOD: Use parameterized queries
const query = 'SELECT * FROM users WHERE name = ?';
db.query(query, [username]);
```

#### **3. Rate Limiting**
```javascript
// Giới hạn số request/giây
const rateLimit = new Map();

socket.on('data', (data) => {
    const clientKey = `${socket.remoteAddress}:${socket.remotePort}`;
    const now = Date.now();
    const requests = rateLimit.get(clientKey) || [];
    
    // Xóa request cũ hơn 1 giây
    const recent = requests.filter(time => now - time < 1000);
    
    if (recent.length >= 10) {
        socket.write('ERROR: Quá nhiều request. Vui lòng thử lại sau.');
        return;
    }
    
    recent.push(now);
    rateLimit.set(clientKey, recent);
    
    // Xử lý request...
});
```

#### **4. Timeout Protection**
```javascript
// Đặt timeout để tránh connection treo
socket.setTimeout(30000);  // 30 giây

socket.on('timeout', () => {
    console.log('Socket timeout. Đóng kết nối.');
    socket.destroy();
});
```

### ✅ Error Handling

```javascript
// ❌ BAD: Không xử lý lỗi
socket.on('data', (data) => {
    const obj = JSON.parse(data);  // Có thể throw exception
    processData(obj);
});

// ✅ GOOD: Xử lý đầy đủ
socket.on('data', (data) => {
    try {
        const obj = JSON.parse(data.toString());
        processData(obj);
    } catch (err) {
        console.error('Parse error:', err.message);
        socket.write(JSON.stringify({
            error: 'Invalid JSON format'
        }));
    }
});

// Bắt tất cả lỗi socket
socket.on('error', (err) => {
    console.error(`Socket error: ${err.message}`);
    // Không throw, sẽ crash app
});

// Server error handling
server.on('error', (err) => {
    if (err.code === 'EADDRINUSE') {
        console.error('Port đã được sử dụng');
    } else {
        console.error('Server error:', err);
    }
});
```

### ✅ Performance Optimization

#### **1. Buffer Pooling**
```javascript
// ❌ BAD: Tạo Buffer mới mỗi lần
socket.on('data', (data) => {
    const response = Buffer.from(processData(data));
    socket.write(response);
});

// ✅ GOOD: Tái sử dụng Buffer
const bufferPool = Buffer.allocUnsafe(1024);
let bufferOffset = 0;

socket.on('data', (data) => {
    const result = processData(data);
    const length = bufferPool.write(result, bufferOffset);
    socket.write(bufferPool.slice(bufferOffset, bufferOffset + length));
    
    bufferOffset = (bufferOffset + length) % 1024;
});
```

#### **2. Batch Processing**
```javascript
// Gộp nhiều message nhỏ thành 1 packet lớn
let messageQueue = [];
let flushTimer = null;

function sendMessage(msg) {
    messageQueue.push(msg);
    
    if (!flushTimer) {
        flushTimer = setTimeout(() => {
            const batch = messageQueue.join('\n');
            socket.write(batch);
            messageQueue = [];
            flushTimer = null;
        }, 10);  // Đợi 10ms để gộp
    }
}
```

#### **3. Streaming Large Data**
```javascript
const fs = require('fs');

// ❌ BAD: Đọc toàn bộ file vào memory
const data = fs.readFileSync('large-file.dat');
socket.write(data);  // Có thể out of memory

// ✅ GOOD: Stream file
const stream = fs.createReadStream('large-file.dat');
stream.pipe(socket);
```

### ✅ Code Organization

```javascript
// Tách logic xử lý ra khỏi network code
class MessageHandler {
    handleCalculation(a, b, c) {
        // Business logic
    }
    
    handleMatrixMultiply(m1, m2) {
        // Business logic
    }
}

class SocketServer {
    constructor(port, handler) {
        this.port = port;
        this.handler = handler;
        this.server = net.createServer(this.handleConnection.bind(this));
    }
    
    handleConnection(socket) {
        socket.on('data', (data) => {
            try {
                const request = JSON.parse(data.toString());
                const result = this.handler.handleRequest(request);
                socket.write(JSON.stringify(result));
            } catch (err) {
                this.handleError(socket, err);
            }
        });
    }
    
    handleError(socket, err) {
        socket.write(JSON.stringify({ error: err.message }));
    }
    
    start() {
        this.server.listen(this.port);
    }
}

// Sử dụng
const handler = new MessageHandler();
const server = new SocketServer(5000, handler);
server.start();
```

---

});
```

#### **UDP Client - Sự khác biệt:**

```javascript
const dgram = require('dgram');
const client = dgram.createSocket('udp4');

// 1. Không cần connect, gửi trực tiếp
function sendData(input) {
    const message = Buffer.from(input);
    
    // 2. Gửi message, phải chỉ định port và host
    client.send(message, 5000, '127.0.0.1', (err) => {
        if (err) {
            console.error('Lỗi:', err);
            client.close();
        } else {
            console.log('Đã gửi');
        }
    });
}

// 3. Lắng nghe phản hồi
client.on('message', (msg) => {
    console.log(`→ ${msg.toString()}`);
});

// Ví dụ sử dụng
sendData('1 -5 6');
```

#### **So sánh TCP vs UDP:**

| Aspect | TCP | UDP |
|--------|-----|-----|
| **Connection** | `client.connect()` | Không cần |
| **Send** | `socket.write(data)` | `socket.send(data, port, host)` |
| **Receive** | `socket.on('data')` | `socket.on('message')` |
| **Server callback** | `createServer((socket) => {})` | Không có callback |
| **Client info** | `socket.remoteAddress` | `rinfo` parameter |
| **Auto response** | `socket.write()` tự biết gửi cho ai | Phải chỉ định `port, address` |

---

## Bài 2: Nhân hai ma trận

### 📝 Mô tả
Client gửi 2 ma trận đến server. Server tính tích 2 ma trận và gửi kết quả về.

### 🚀 Cách chạy
```bash
node 2_tcp_server.js  # hoặc 2_udp_server.js
node 2_tcp_client.js  # hoặc 2_udp_client.js
```

### 💡 Ví dụ
```
A (r c): 2 2
Nhập 4 số: 1 2 3 4

B (r c): 2 2
Nhập 4 số: 5 6 7 8

A×B:
  19  22
  43  50
```

### 🔍 Giải thích thuật toán nhân ma trận

**Công thức:** `C[i][j] = Σ(A[i][k] × B[k][j])`

```javascript
function multiply(A, B) {
    const m = A.length;        // Số hàng A
    const n = B.length;        // Số cột A = số hàng B
    const p = B[0].length;     // Số cột B
    
    const C = [];
    for (let i = 0; i < m; i++) {
        C[i] = [];
        for (let j = 0; j < p; j++) {
            C[i][j] = 0;
            // Tính tổng tích
            for (let k = 0; k < n; k++) {
                C[i][j] += A[i][k] * B[k][j];
            }
        }
    }
    return C;
}
```

**Ví dụ cụ thể:**
```
A = [1 2]    B = [5 6]
    [3 4]        [7 8]

C[0][0] = 1×5 + 2×7 = 5 + 14 = 19
C[0][1] = 1×6 + 2×8 = 6 + 16 = 22
C[1][0] = 3×5 + 4×7 = 15 + 28 = 43
C[1][1] = 3×6 + 4×8 = 18 + 32 = 50

Kết quả: [19 22]
         [43 50]
```

### 🔍 Cách gửi ma trận qua mạng

**Sử dụng JSON:**
```javascript
// Client gửi
const data = JSON.stringify({
    m1: [[1,2],[3,4]],
    m2: [[5,6],[7,8]]
});
socket.write(data);

// Server nhận
const { m1, m2 } = JSON.parse(data.toString());
```

---

## Bài 3: Thay thế chuỗi trong file

### 📝 Mô tả
Client gửi:
- Nội dung file text
- Chuỗi cần tìm (s1)
- Chuỗi thay thế (s2)

Server thay thế tất cả `s1` bằng `s2` và gửi file kết quả về.

### 🚀 Cách chạy
```bash
node 3_tcp_server.js  # hoặc 3_udp_server.js
node 3_tcp_client.js  # hoặc 3_udp_client.js
```

### 💡 Ví dụ

**File test.txt:**
```
Hello world
This is a test file
Hello everyone
```

**Thực hiện:**
```
Đường dẫn file: test.txt
Chuỗi cần thay thế (s1): Hello
Chuỗi thay thế (s2): Hi
Lưu kết quả vào file: result.txt
✓ Đã thay thế 2 lần
```

**File result.txt:**
```
Hi world
This is a test file
Hi everyone
```

### 🔍 Giải thích code

**Đọc file:**
```javascript
const fs = require('fs');

// Đọc file đồng bộ
const content = fs.readFileSync('test.txt', 'utf8');
```

**Thay thế chuỗi:**
```javascript
function replaceAll(content, s1, s2) {
    // Cách 1: Dùng split và join
    return content.split(s1).join(s2);
    
    // Cách 2: Dùng replaceAll (ES2021)
    // return content.replaceAll(s1, s2);
}
```

**Đếm số lần xuất hiện:**
```javascript
const count = content.split(s1).length - 1;
```

**Ghi file:**
```javascript
fs.writeFileSync('result.txt', newContent, 'utf8');
```

---

## Bài 4: Xử lý chuỗi

### 📝 Mô tả
Server cung cấp các lệnh xử lý chuỗi:
- `UPPER`: Chuyển chữ thường → HOA
- `LOWER`: Chuyển chữ HOA → thường
- `DELETE`: Xóa một phần chuỗi
- `INSERT`: Chèn chuỗi con

### 🚀 Cách chạy
```bash
node 4_tcp_server.js  # hoặc 4_udp_server.js
node 4_tcp_client.js  # hoặc 4_udp_client.js
```

### 💡 Ví dụ các lệnh

```bash
# Chuyển HOA
Nhập lệnh: UPPER|hello world
→ HELLO WORLD

# Chuyển thường
Nhập lệnh: LOWER|HELLO WORLD
→ hello world

# Xóa ký tự từ vị trí 0 đến 5
Nhập lệnh: DELETE|hello world|0|5
→  world

# Chèn " beautiful" vào vị trí 5
Nhập lệnh: INSERT|hello world|5| beautiful
→ hello beautiful world
```

### 🔍 Giải thích code

**Protocol thiết kế:**
```
Format: COMMAND|text|param1|param2
Phân tách bằng dấu |
```

**Server xử lý:**
```javascript
function processCommand(cmd, text, ...params) {
    switch(cmd) {
        case 'UPPER':
            return text.toUpperCase();
        
        case 'LOWER':
            return text.toLowerCase();
        
        case 'DELETE':
            const [start, end] = params.map(Number);
            // Cắt từ 0->start, nối với end->cuối
            return text.slice(0, start) + text.slice(end);
        
        case 'INSERT':
            const pos = Number(params[0]);
            const substr = params[1];
            // Cắt 0->pos, chèn substr, nối pos->cuối
            return text.slice(0, pos) + substr + text.slice(pos);
        
        default:
            return 'Lệnh không hợp lệ';
    }
}
```

**Client gửi:**
```javascript
// Parse input
const parts = data.toString().split('|');
const [cmd, text, ...params] = parts;

// Xử lý
const result = processCommand(cmd, text, ...params);
```

---

## Bài 5: Chat room

### 📝 Mô tả
Tạo phòng chat cho phép nhiều máy tính trao đổi thông tin. Mỗi tin nhắn được broadcast đến tất cả client khác.

### 🚀 Cách chạy

```bash
# Terminal 1: Server
node 5_tcp_server.js  # hoặc 5_udp_server.js

# Terminal 2, 3, 4...: Nhiều client
node 5_tcp_client.js  # hoặc 5_udp_client.js
```

### 💡 Ví dụ

**Terminal 1 (Alice):**
```
Nhập tên: Alice
[SERVER] Alice đã tham gia chat!
> Hello everyone!
✓ Đã gửi
[Bob]: Hi Alice!
```

**Terminal 2 (Bob):**
```
Nhập tên: Bob
[Alice]: Hello everyone!
> Hi Alice!
✓ Đã gửi
```

### 🔍 Giải thích code

**Server quản lý danh sách client:**
```javascript
const clients = [];  // Mảng lưu tất cả socket

server.on('connection', (socket) => {
    // Thêm client mới
    clients.push({ socket, username });
    
    socket.on('data', (data) => {
        const message = data.toString();
        
        // Broadcast cho tất cả (trừ người gửi)
        broadcast(message, socket);
    });
    
    socket.on('close', () => {
        // Xóa client khi disconnect
        const index = clients.indexOf(socket);
        clients.splice(index, 1);
    });
});
```

**Hàm broadcast:**
```javascript
function broadcast(message, exclude = null) {
    clients.forEach(client => {
        // Không gửi lại cho người gửi
        if (client.socket !== exclude) {
            client.socket.write(message + '\n');
        }
    });
}
```

**UDP Chat (khác biệt):**
```javascript
// UDP không có "connection", phải track clients thủ công
const clients = new Map();  // Map<"ip:port", username>

server.on('message', (msg, rinfo) => {
    const key = `${rinfo.address}:${rinfo.port}`;
    
    // Lưu thông tin client
    clients.set(key, username);
    
    // Broadcast
    clients.forEach((username, clientKey) => {
        const [addr, port] = clientKey.split(':');
        server.send(msg, parseInt(port), addr);
    });
});
```

---

## So sánh TCP vs UDP

### 📊 Bảng so sánh

| Tiêu chí | TCP | UDP |
|----------|-----|-----|
| **Kết nối** | Có (3-way handshake) | Không |
| **Độ tin cậy** | Đảm bảo gửi đến | Không đảm bảo |
| **Thứ tự** | Đúng thứ tự | Có thể sai thứ tự |
| **Tốc độ** | Chậm hơn | Nhanh hơn |
| **Overhead** | Lớn | Nhỏ |
| **Module Node.js** | `net` | `dgram` |
| **Tạo server** | `net.createServer()` | `dgram.createSocket('udp4')` |
| **Gửi dữ liệu** | `socket.write()` | `socket.send(msg, port, host)` |
| **Nhận dữ liệu** | `socket.on('data')` | `socket.on('message')` |
| **Use case** | File transfer, Chat | Streaming, Gaming |

### 💻 Code so sánh

**TCP:**
```javascript
// Server
const net = require('net');
const server = net.createServer((socket) => {
    socket.on('data', (data) => {
        socket.write('Response');
    });
});
server.listen(5000);

// Client
const client = new net.Socket();
client.connect(5000, '127.0.0.1');
client.write('Hello');
```

**UDP:**
```javascript
// Server
const dgram = require('dgram');
const server = dgram.createSocket('udp4');
server.on('message', (msg, rinfo) => {
    server.send('Response', rinfo.port, rinfo.address);
});
server.bind(5000);

// Client
const client = dgram.createSocket('udp4');
const msg = Buffer.from('Hello');
client.send(msg, 5000, '127.0.0.1');
```

---

## 🎯 Khi nào dùng TCP vs UDP?

### Dùng TCP khi:
- ✅ Cần đảm bảo dữ liệu đến đích
- ✅ Thứ tự tin nhắn quan trọng
- ✅ Ví dụ: Chat, file transfer, email

### Dùng UDP khi:
- ✅ Tốc độ quan trọng hơn độ tin cậy
- ✅ Chấp nhận mất một ít dữ liệu
- ✅ Ví dụ: Video streaming, game online, DNS

---

## 🔧 Troubleshooting

### Lỗi thường gặp:

**1. `EADDRINUSE` - Port đã được sử dụng**
```bash
# Tìm process đang dùng port
netstat -ano | findstr :5000

# Kill process (Windows)
taskkill /PID <PID> /F
```

**2. `ECONNREFUSED` - Không kết nối được**
- Kiểm tra server đã chạy chưa
- Kiểm tra port và IP đúng không
- Kiểm tra firewall

**3. Client không nhận được dữ liệu**
- TCP: Kiểm tra `socket.write()` và `socket.on('data')`
- UDP: Kiểm tra `socket.send()` và `socket.on('message')`

---

## 📚 Tài liệu tham khảo

- [Node.js Net Documentation](https://nodejs.org/api/net.html)
- [Node.js Dgram Documentation](https://nodejs.org/api/dgram.html)
- [TCP/IP Protocol](https://en.wikipedia.org/wiki/Transmission_Control_Protocol)
- [UDP Protocol](https://en.wikipedia.org/wiki/User_Datagram_Protocol)

---

## ✅ Checklist học tập

- [ ] Hiểu khác biệt TCP vs UDP
- [ ] Biết cách tạo server/client với `net` và `dgram`
- [ ] Biết cách gửi/nhận dữ liệu
- [ ] Biết serialize/deserialize data (JSON)
- [ ] Hiểu event-driven programming
- [ ] Biết xử lý nhiều client (broadcast)
- [ ] Biết xử lý lỗi và disconnect

---

**🎉 Chúc bạn học tốt!**
